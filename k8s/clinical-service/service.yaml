apiVersion: v1
kind: Service
metadata:
  name: clinical-service
  namespace: medicate
spec:
  selector:
    app: clinical-service
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP